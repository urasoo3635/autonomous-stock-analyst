# =============================================================================
# 環境変数テンプレート
# このファイルをコピーして .env を作成してください
# cp .env.example .env
# .env を Git にコミットしないこと（.gitignore に追加済み）
# =============================================================================

# --- アプリケーション設定 ---
ENVIRONMENT=dev                        # dev | staging | prod
APP_VERSION=0.1.0
DEBUG=false
LOG_LEVEL=INFO

# --- データベース ---
DATABASE_URL=postgresql://dbadmin:devpassword@db:5432/stock_analyst

# --- セキュリティ ---
# `python -c "import secrets; print(secrets.token_urlsafe(32))"` で生成
SECRET_KEY=your-secret-key-here
# 本番環境では許可するホストを列挙する (例: "stock-analyst.example.com")
ALLOWED_HOSTS=["localhost","127.0.0.1"]
# 本番環境では許可するオリジンを列挙する (例: "https://stock-analyst.example.com")
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
# 1分あたりのレートリミット
RATE_LIMIT_PER_MINUTE=60

# --- 外部 API ---
# FRED (連邦準備制度) API キー: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your-fred-api-key-here
